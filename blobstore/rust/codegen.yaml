schema: ../blobstore.widl
generates:
  src/generated.rs:
    module: '@wapc/widl-codegen/rust'
    visitorClass: ModuleVisitor
    config:
      hostRoles:
        - Store
      handlerRoles:
        - Actor