schema: ../blobstore.widl
generates:
  assembly/index.ts:
    module: '@wapc/widl-codegen/assemblyscript'
    visitorClass: ModuleVisitor
    config:
      hostRoles:
        - Store
      handlerRoles:
        - Actor