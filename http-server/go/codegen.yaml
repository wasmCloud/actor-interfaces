schema: ../httpserver.widl
generates:
  httpserver.go:
    module: '@wapc/widl-codegen/tinygo'
    visitorClass: ModuleVisitor
    config:
      package: httpserver
      handlerRoles:
        - Actor
